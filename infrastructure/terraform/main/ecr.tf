# For POC purposes, it has been created by the GHA pipeline on first run

# resource "aws_ecr_repository" "main" {
#   name                 = "${local.eks_name}-${var.project}-app"
#   image_tag_mutability = "MUTABLE"
# }

